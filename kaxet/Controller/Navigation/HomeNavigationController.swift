//
//  HomeNavigationController.swift
//  kaxet
//
//  Created by LEONARD GURNING on 23/10/18.
//  Copyright © 2018 LEONARD GURNING. All rights reserved.
//

import UIKit

class HomeNavigationController: BaseNavigationController {
    /*
    override var miniPlayerVc: MiniPlayerViewController? {
        didSet{
            if let tvc = self.topViewController as? HomeViewController {
                tvc.miniPlayerVc = miniPlayerVc
            } else if let tvc = self.topViewController as? GenreSongsViewController {
                tvc.miniPlayerVc = miniPlayerVc
            }
        }
    }
    
    override var miniPlayerView: UIView? {
        didSet{
            if let tvc = self.topViewController as? HomeViewController {
                tvc.miniPlayerView = miniPlayerView
            } else if let tvc = self.topViewController as? GenreSongsViewController {
                tvc.miniPlayerView = miniPlayerView
            }
        }
    }
    */
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
